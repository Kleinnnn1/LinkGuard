generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Scan {
  id        Int      @id @default(autoincrement())
  url       String
  result    String
  createdAt DateTime @default(now())
}